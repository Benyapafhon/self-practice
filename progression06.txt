ให้chatGPTสร้างโจทย์ 
ฟังก์ชั่น makeMultiplier รับค่า x แล้วreturnฟังก์ชั่นตัวข้างในที่รับค่า y
ออกมาโดยที่มันสามารถเข้าถึงตัวค่า  x ที่อยู่ใน makeMultiplier 
แล้วเราก็ทำการสร้างฟังก์ชั่นใหม่ที่ชื่อว่า times3 โดยให้ makeMultiplier
คืนค่า  function(y) ที่ตอนนี้มันจำค่าว่าตัวเองค่า x=3 ทำให้ มันออกมาเป็น 3+y
แล้วเราก็ console.log(times3(5)); คือ times3(5) เอาตัว x=3 * y=3 ทำให้ได้ 15