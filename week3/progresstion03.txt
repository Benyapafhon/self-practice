ฟังก์ชันนี้คือการ เอาคำในลิสต์มาเปลี่ยนเป็นตัวใหญ่ทุกคำ โดยarray.map(str => str.toUpperCase())
วนลูปทุกตัวใน array แล้วทำอะไรสักอย่างกับมัน str => str.toUpperCase() คือเอาคำ(str) แต่ละตัวมาเปลี่ยนเป็น ตัวพิมพ์ใหญ่