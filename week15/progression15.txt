นี่คือโค้ดส่วนหนึ่งของโค้ดนี้เป็นส่วนหนึ่งของ integrated project  มันจะทำหน้าที่แสดงเวลาที่เปิดทำการของการจอง และเวลาถัดไป
เริ่มจากทำการ ไปเอาเวลามาโดยการ fetch ไปที่reservation-periods และทำการเอาค่ามาเก็บไว้ที่ตัวแปร time จัดการพวกerror
ด้วยการใช้ try catch และก็ทำการเอาตัวtimeที่เก็บค่ามาปลงเป็น json แล้วเก็บใน  result เพื่อนำไปใช้ต่อ และนำตัวฟังก์ชั่น gettime
มาใช้ต่อในข้าง แล้วก็มาต่อที่ ก้อนแรกคือ currentPeriodStatus คือcheck ตัวเวลาตอนนี้ถ้าไม่ใช่เวลาตอนนี้ก้จะทำการ เปลี่ยนตรง status ให้  closed
และ add class เข้าไปเพื่อใช้ classแต่ถ้ามันคือช่วงเวลาตอนนี้ให้เปิด ลบ class close ก่อนแล้วให้กลับมา open 
แล้วแสดงเวลาเริ่มและเวลาจบ นำออกมาเป็นtextcontent addเข้าไปใน html และcheck ตัวnext period ถ้าเกิดว่าไม่มีก็จะขึ้นบอกว่าไม่มีในเวลาถัดไป 
และแสดง status เป้น closed แต่ถ้ามีก็แสดงออกมาตามปกติ